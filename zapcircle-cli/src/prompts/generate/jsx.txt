Based on the following behavior description, generate a React component.

${global_prompt}

${generate_prompt}

${behavior}

The output should only contain the component implementation in JSX format. 

Do not include comments, markdown, or any other text. 

Do not include the ``` code delimiters for markdown.

The output should be ready to run as a React component.